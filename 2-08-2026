1.QUESTION: Create a table from student and courses table
create table student(
rollNo int primary keys, auto_increment,
studentName varchar(200)
);
create table courses
courseId int,
courseName varchar(200),
rollno int,
foriegn keys(rollno)reference courses(rollno)
);
insert into student(rollNo,studentName)
(1,'anusha'),
(2,'gopi'),
(3,'tharkesh'),
(4,'karthik');
select * from student;
insert into courses(courseId,courseName,rollno) values
(1,'DBMS',1),
(2,'OS,2),
(3,'computer networks',3),
(4,'SQL',4);
select * from courses;
select * from student inner join courses on student.rollno=courses.rollno;
select * from student left join courses on student.rollno=courses.rollno;
select * from student right join courses on student.rollno=courses.rollno;
select * from student natural join courses;
select * from student cross join courses order by student.studentName;
----------------------------------------------------------------------------------------------------
2.QUESTION: Write a query to do the following:
FULL OUTER JOIN the student and course tables using 'Course_id' to match the tables. Output the joined table.
select * from student
full join course on student.course_id=course.course_id;
------------------------------------------------------------------------------------------------------------------
3.QUESTION: Write the queries to do the following:
JOIN the tables 'student' and 'course' using 'Course_id' to match both the tables and output the joined table.
RIGHT JOIN the tables 'student' and 'course' using 'Course_id' to match both the tables and output the joined table.
select * from student
 join course on student.course_id=course.course_id;
select * from student
right join course on student.course_id= course.course_id;
----------------------------------------------------------------------------------------------------------------------------
4.QUESTION: Write a query to do the following
Cross join the table 'student' and 'course' and check out all possible courses a student can opt.
Output only two columns, the name of the student as 'St_Name' and courses as 'Course_Name'.
select St_name,Course_Name from student cross join course;
------------------------------------------------------------------------------------------------------------------------------
